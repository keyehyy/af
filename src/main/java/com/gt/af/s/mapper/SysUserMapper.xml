<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gt.af.s.mapper.SysUserMapper">



    <select id="selectSysUser" resultType="com.gt.af.s.model.SysUser">
       select id,username,password,last_login_time,create_time,update_time,create_user,update_user,account_status from sys_user
       <where>
           <if test="username != null and username != ''">
               AND username = #{username}
           </if>
           <if test="id != null and id != ''">
               AND id = #{id}
           </if>
       </where>
    </select>

    <insert id="insertSysUser" keyProperty="id" useGeneratedKeys="true">
      INSERT INTO sys_user (username,password,create_time,create_user,account_status)
      VALUES  (#{username},#{password},now(),#{create_user},'1')
    </insert>

</mapper>